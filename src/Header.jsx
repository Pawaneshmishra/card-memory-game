const Header = () => {
    return (
        <header>
            <h1>Pokemon Memory Game</h1>
            <p>Click on each Pokemon once. Don't click the same Pokemon twice!</p>
        </header>
    );
}

export default Header;